{
  "name": "lc-testv1",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ needavailablename/lc-testv1",
    "create-local": "graph create --node http://localhost:8020/ needavailablename/lc-testv1",
    "remove-local": "graph remove --node http://localhost:8020/ needavailablename/lc-testv1",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 needavailablename/lc-testv1",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.50.1",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
